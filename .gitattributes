# Keep the current baseline line endings for files that already carry
# non-normalized EOLs, otherwise Git will expand the patch with EOL-only noise.
_conf_schema.json -text
core/retrieval/graph_vector_retriever.py -text